<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Staff Management - Empty State</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            background-color: #f8f9fa;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
        }

        .empty-state {
            text-align: center;
            max-width: 600px;
        }

        .icon-circle {
            width: 80px;
            height: 80px;
            border: 4px solid #6b7280;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 32px;
        }

        .x-icon {
            width: 40px;
            height: 40px;
            position: relative;
        }

        .x-icon::before,
        .x-icon::after {
            content: '';
            position: absolute;
            width: 4px;
            height: 40px;
            background-color: #6b7280;
            top: 0;
            left: 18px;
        }

        .x-icon::before {
            transform: rotate(45deg);
        }

        .x-icon::after {
            transform: rotate(-45deg);
        }

        .message {
            font-size: 18px;
            color: #6b7280;
            line-height: 1.6;
            margin-bottom: 32px;
        }

        .button-group {
            display: flex;
            gap: 16px;
            justify-content: center;
            flex-wrap: wrap;
        }

        .btn {
            padding: 14px 28px;
            font-size: 16px;
            font-weight: 600;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.2s ease;
            font-family: inherit;
        }

        .btn-primary {
            background-color: #3d7d6c;
            color: white;
        }

        .btn-primary:hover {
            background-color: #2d6858;
        }

        .btn-secondary {
            background-color: white;
            color: #374151;
            border: 2px solid #d1d5db;
        }

        .btn-secondary:hover {
            background-color: #f9fafb;
            border-color: #9ca3af;
        }
    </style>
</head>
<body>
    <div class="empty-state">
        <div class="icon-circle">
            <div class="x-icon"></div>
        </div>
        
        <p class="message">
            You currently do not have any staff. Add new staff or<br>
            import existing records to see staff list here.
        </p>
        
        <div class="button-group">
            <button class="btn btn-primary">Add New Staff</button>
            <button class="btn btn-secondary">Import CSV File</button>
        </div>
    </div>
</body>
</html>